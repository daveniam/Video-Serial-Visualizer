<Window x:Class="VideoSerialVisualizer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:VideoSerialVisualizer"
        mc:Ignorable="d"
        Title="Video Serial Visualizer" Height="720" Width="1200" WindowStartupLocation="CenterScreen"
        Background="{StaticResource BgPrimaryBrush}" Icon="Assets/AppIcon.ico">
    <Grid>
        <ContentControl Content="{Binding CurrentViewModel}" />

        <!-- Sello de version, siempre presente de forma discreta en la esquina inferior derecha.
             No intercepta clics (IsHitTestVisible=False) y se atenua para no competir con el
             contenido. Se oculta en el reproductor (ver IsVersionBadgeVisible): esa esquina la
             ocupa la barra de controles. El texto lo pone MainWindow.xaml.cs desde la version del
             ensamblado, para no duplicar el numero. -->
        <TextBlock x:Name="VersionBadge" IsHitTestVisible="False"
                   HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,0,12,8"
                   Foreground="{StaticResource TextSecondaryBrush}" Opacity="0.4"
                   FontSize="11"
                   Visibility="{Binding IsVersionBadgeVisible, Converter={StaticResource BooleanToVisibilityConverter}}" />

        <Border Background="{StaticResource BgPrimaryBrush}"
                Visibility="{Binding IsLoading, Converter={StaticResource BooleanToVisibilityConverter}}">
            <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center">
                <Grid Width="48" Height="48" HorizontalAlignment="Center">
                    <Ellipse Width="48" Height="48" StrokeThickness="4" Stroke="{StaticResource AccentBrush}"
                             StrokeDashArray="2 1.4" StrokeStartLineCap="Round" StrokeEndLineCap="Round">
                        <Ellipse.RenderTransform>
                            <RotateTransform CenterX="24" CenterY="24" />
                        </Ellipse.RenderTransform>
                        <Ellipse.Triggers>
                            <EventTrigger RoutedEvent="Ellipse.Loaded">
                                <BeginStoryboard>
                                    <Storyboard>
                                        <DoubleAnimation
                                            Storyboard.TargetProperty="(Ellipse.RenderTransform).(RotateTransform.Angle)"
                                            From="0" To="360" Duration="0:0:1" RepeatBehavior="Forever" />
                                    </Storyboard>
                                </BeginStoryboard>
                            </EventTrigger>
                        </Ellipse.Triggers>
                    </Ellipse>
                </Grid>
                <TextBlock Text="{Binding LoadingMessage}" Foreground="{StaticResource TextSecondaryBrush}"
                           Margin="0,16,0,0" HorizontalAlignment="Center" />
            </StackPanel>
        </Border>
    </Grid>
</Window>
